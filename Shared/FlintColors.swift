import SwiftUI

extension Color {
    // Pitch black / dark charcoal theme
    static let flintBackground = Color(red: 0.05, green: 0.04, blue: 0.04) 
    static let flintRed = Color(red: 0.88, green: 0.24, blue: 0.10) // Rich orange-red from screenshots
    static let flintGlass = Color.white.opacity(0.06) // Glass fill
    static let flintCardBorder = Color.white.opacity(0.08) // Subtle border color
    static let flintGlow = Color(red: 0.88, green: 0.24, blue: 0.10).opacity(0.4) // Glow color
}
